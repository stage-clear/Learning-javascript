# Range.js

```js
/**
 * Range
 * @param {Number} n
 * @return {Array}
 * @reference [関数型プログラミング目覚めた(P106)]
 */

function range(n) {
  let = tmp = [];
  for (let i = 0; i < n; i += 1) {
    tmp[i] = i;
  }
  return tmp;
}
```
